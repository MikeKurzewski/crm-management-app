"use client";

import React from "react";
// import { Button } from "@/components/ui/button";
// import { usePathname, useRouter } from "next/navigation";
// import { Building } from "lucide-react";

export function AppNav({
  activeOrgId,
}: {
  activeOrgId: string;
}) {
  // Placeholder: implement nav with active state, e.g., using usePathname
  return (
    <nav>
      {/* <Button ... /> */}
      <div className="text-muted-foreground">AppNav navigation coming soon</div>
    </nav>
  );
}

export default AppNav;
